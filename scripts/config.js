import saveManager from "./saveManager.js";

const systemSettings = {
  skipBootScreen: {
    state: false,
    accessibleFromMenu: true
  },
  appEnabled: {
    state: true
  }
}

export default systemSettings;